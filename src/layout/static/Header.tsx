const Header = () => {
    return(
        <header id="header-component">
            <h1 className="header-title">
                SismoLab
            </h1>
        </header>
    );
};

export default Header;