import type { SidebarGroupConfig } from "../models/interfaces/sidebar/SidebarGroupConfig";

/* Here we have all the sidebar group for renderizing and adding its equivalent routes. */
export const sidebarGroups: SidebarGroupConfig[] = [
    {
        group: "events",
        title: "Eventos"
    }
]