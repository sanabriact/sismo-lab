const Loading = () => {
    return (
        <div className="flex flex-col items-center justify-center min-h-screen gap-4">
            <span className="text-6xl font-bold text-gray-200 dark:text-gray-700">Cargando...</span>
        </div>
    );
};

export default Loading;